# angularjs e2e httpbackend utility

Takes javascript objects and maps them to "restful" URLs using angulars mocke2e
httpbackend. All future actions against the objects is persisted in local
storage.
